/* Movies interface to store the values that are in the JSON objects */
export interface Movies {
  id: number,
  title: string,
  poster: string,
  runtime: string,
  mpaRating: string,
  year: number
}
